name: server
runtime:
  name: python
  options:
    toolchain: pip
    # virtualenv: venv
description: A minimal Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: python
plugins:
  providers:
    - name: upcloud
      path: ../../bin/
